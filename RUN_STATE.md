# RUN_STATE — ORRERY

> 🧠 **REHYDRATION / FULL RECOLLECTION:** for the complete session memory + continuation prompt + rehydration procedure, read **[`SESSION_MEMORY.md`](SESSION_MEMORY.md)** first. Trust the files + git over any recalled narrative; verify with `git log/status` and a tool's `--selftest`/`--golden` before resuming. History of the 6-tool build (2026-07-05/06): git log + `runs/*_twopass_verify.md`.

## `lens` v1.0.0 SHIPPED (2026-07-12) — the parked SPIKE's render arm, built to full standard
The backlog `lens` (D-004) is built: a deterministic **OptiX RT-core silhouette renderer + oracle-gated cross-section** tool (9th in the catalogue). Honest scope per D-004 (**render only**; the RT-as-compute isomorphism is a pre-registered SPIKE in the contract with a ≥1.5× kill). Two engines measure the same declared cross-section: a CPU fp64 **analytic baseline** (declared anchor + I-11 oracle, driver-independent) and the **OptiX RT path** (I-13 paired-oracle cross-check, gate G-RT-DIVERGE). Scenes: `sphere` (πR²) + `bhshadow` (Schwarzschild capture **27π M²** at b_crit=√27 M). Golden `11e545b8` (bhshadow 1024², both engines): det. **3× byte-identical on the sm_89+OptiX 9.1.0+drv 610.47 pin**; area_rel_err 2.8e-5; hit_pixels_rt == hit_pixels == 366012 (rt_baseline_delta=0). Selftest 15/15; **harness GREEN (cold rebuild + selftest + golden)**. Contract/schema/MODULE/golden/NOTE/result.lock all frozen; D-029 logged; ARCHITECTURE §8 + tools/README registered. OptiX toolchain learnings: bare-nvcc needs `-lcuda -ladvapi32`; PTX embedded via `embed_ptx.py` (self-contained exe). **Cold two-pass: CONFORMANT** (0 defects; an independent no-build-context agent cold-rebuilt from source, reproduced golden `11e545b8` byte-identical, verified hash computed-not-stamped via tamper/M² law, schema + exit codes 0/1/2 + I-11/I-13 oracles + firewall + no-fast-math all pass — `runs/lens_twopass_verify.md`). **The Wave-0 standard is met; lens is fully cleared.**

## lens compute-SPIKE RULED (2026-07-12, D-030) — D-004's Carmack question, RETIRED by measurement
Continued into the pre-registered compute-SPIKE. Built the honest fp64 CUDA curved-geodesic baseline (`tools/lens/_spike_geodesic/geodesic.cu`): it integrates the exact Schwarzschild null-geodesic Binet equation and **DERIVES** the shadow (σ=27π M², b_crit=√27 M, validated to **2.5e-6** — v1 hard-coded that silhouette; the baseline derives it from the metric) and renders the gravitationally-lensed black-hole image (photon ring + celestial lensing). **RT-cores-as-isomorphic-compute RETIRED:** measured RT throughput 5.89e9 traces/sec vs the fp64 baseline's 1.013e7 ray-integrations/sec; at matched accuracy (≈6000 traces/ray) RT = 9.8e5 rays/sec → **baseline ~10× faster; ≥1.5× kill fires** (ADR-007 in-house: RT accelerates intersection, not ODE arithmetic; trivial scene → BVH wins nothing). **D-004's oldest open question is now resolved (measured, not asserted).** The geodesic baseline is a **candidate lens v1.1.0 MINOR** (`--geodesic` derive-the-shadow + lensed render) — graduation operator-gated. Memo: `runs/lens_spike_geodesic.md`; spike code kept as evidence + graduation seed (contract SPIKE section carries the resolution; v1.0.0 golden unchanged).

## lens v1.1.0 SHIPPED (2026-07-12, D-031) — the geodesic baseline GRADUATED to a real tool
Additive MINOR: new scene **`bhshadow-geo`** DERIVES the Schwarzschild shadow by integrating real null geodesics (Binet, GPU fp64, pinned φ-steps) instead of the hard-coded silhouette; cross-checked against the OptiX b_crit silhouette (I-13) + a gravitationally-lensed `--render` (the shipped tool renders the photon-ring image). **Additive-safe:** no declared field/flag/exit-code changed (only the scene enum + PINNED internal integration constants, deliberately not declared params) → the v1.0.0 golden `11e545b8` reproduces **BYTE-IDENTICAL**. Triple-validated: `hit_pixels=366012` (geodesic) = `hit_pixels_rt=366012` (OptiX silhouette) = analytic; `area_rel_err 2.8e-5` vs 27π M². Second golden `914399…` (`goldens/lens/declared_geo.hash`); `--golden` checks BOTH. Selftest 19/19; harness GREEN (cold rebuild, both goldens); **cold two-pass CONFORMANT (0 defects, 10/10)** — `runs/lens_v1_1_0_twopass_verify.md`. Contract v1.1.0 + schema + MODULE + NOTE + result.lock + D-031 + ARCHITECTURE §8 + tools/README updated. **lens = DONE v1.1.0.**

## Current state
**Wave-0 (Phase 5) session, 2026-07-09.** Operator adopted the wave plan (`docs/PROPOSAL_2026-07-09_wave_plan.md`): D-020 (liborrery) + D-021 (CMake preset/fat binary/fast-math ban) ACTIVE; I-11..I-14 in ARCHITECTURE §5; Phase 5–8 addendum in TASKLIST. D-022..D-026 stay PROPOSED until their phases open.

**Bootstrap verification (this session, before any change):** all 6 tools cold-green — selftests exit 0 (someone, ratchet, posit, mcts, algebra, autotune) and goldens exit 0 (ratchet, mcts, algebra, posit, autotune fast; someone `aa5b731d` reproduced in ~8 min, GOLDEN OK).

**`lib/` (liborrery v1.0.0) BUILT + KAT-GREEN (D-020).** envelope.h/.cpp (blake2b + sha256 + canonical serializers + golden plumbing + CLI spine + lock writer; D-013 hash domain unchanged), rng.cuh (D-012 kit), reduce.cuh (fixed-order reductions + order-invariant fixed-point atomics + sort-then-gather host ref), regime.h (derived-only bitmask), ckpt.h (B7: dump + sha256 sidecar + verified resume), MODULE.md, selftest.cu → `orrery_selftest.exe` **42/42 PASS** (run from repo root). Extraction is VERBATIM from someone v1.1.0, enforced mechanically by a ref-namespace cross-check + pinned host/device RNG bit patterns (integer/u01 pins independently confirmed in Python; splitmix64(0) = Vigna's published vector).

**Measured finding, now pinned:** host (MSVC) vs device (CUDA) libm diverge by **1 ULP** on `counter_gauss(20260705,7,11,13)` (`...9d3b` vs `...9d3a`). Never assert host==device on transcendental paths; the device pins are a CUDA-toolkit drift detector that fires before a tool golden silently breaks.

**CMake preset (D-021) VERIFIED:** `cmake --preset windows-sm89-fat` + build green (CMake 4.3.3 + VS Ninja under vcvars64); cuobjdump shows sm_89+sm_90+sm_120 SASS + sm_120 PTX; CMake-built selftest passes. Bare-nvcc stays the golden path; `build/` gitignored.

## Migrations: ALL FOUR GREEN (2026-07-09, one commit each)
**ratchet v1.0.1** golden `91fce3c4` ✓ bit-identical 3× · **mcts v1.0.1** `6c596a53` ✓ 3× · **algebra v1.0.1** `1526918f` ✓ 3× · **someone v1.1.1** `aa5b731d` ✓ (~8-min full-precision run, byte-for-byte). Zero mismatches — no SUSPECT, no re-baseline. Harness `verify.py --tool` GREEN for ratchet/mcts/algebra; posit/autotune untouched and re-confirmed golden-green. **D-020's acceptance criterion ("the code is ephemeral", in vivo) is fully met.** Net: each tool lost its ~80–220 lines of duplicated core; the doctrine now lives in one KAT-pinned place.

## `mcp` v1.0.0 BUILT + VERIFIED (tool #7, D-022 adopted Active) — the instrument is LLM-callable
Contract v1.0.0 + schema + MODULE + `mcp.py` (stdio JSON-RPC 2.0; six tools: list_tools, describe_contract-verbatim, run_tool, get_run, sweep→autotune, golden_status). **I-12 live:** every run response embeds the D-013 declared blake2b (textual extraction from the fixed-order envelope) + artifact blake2b. Golden `174ec02d` (3× byte-identical; the canned-posit chain — deliberate narrow coupling, re-baseline protocol in goldens/mcp/NOTE.md). Selftest 13/13. Polyglot harness GREEN. Live smoke drove a real ratchet GPU run through --serve. Pre-commit smoke caught + fixed a real defect (param keys rejected the catalogue's uppercase --R/--N). **Cold two-pass: CONFORMANT 10/10, no defects** (`runs/mcp_twopass_verify.md`).

## `orreryd` v0.1.0 BUILT + VERIFIED (tool #8, D-022 v0) — the unattended-run surface
C++20 on liborrery (first NEW lib consumer), no threads. File-spool serializer: one GPU tenant FIFO, per-job budgets (live-proven 2s kill), `.stop`/`.DONE` sentinels, atomic status page, I-12 hashes on every record. Golden `86f133bb` (3×; canned 3-job drain: order + error containment + posit chain + `.DONE`). Selftest 13/13; harness GREEN; live smokes (real GPU drain, budget kill). **Cold two-pass: CONFORMANT 13/13, no defects — incl. cold rebuild from source reproducing the golden** (`runs/orreryd_twopass_verify.md`). v0 exclusions documented (CUDA-stream tenancy, HTTP API, mcp enqueue).

## `/lab` PAGE BUILT (local; NOT deployed) — **PHASE 5 IS COMPLETE (2026-07-09)**
`_build_lab.py` in `C:\Websites\finaltheoryofeverything.org\` renders `_upload/lab.html` from the **live registry via the mcp surface** (the website is a caller, like the science — no ORRERY internals read) + the repo's two-pass verdict artifacts; skin = the frozen v4 backup (idempotent, the v5 builder pattern). Structural battery ALL PASS: 8 tools, 8/8 two-pass CONFORMANT, all golden hashes live-matched, firewall verbatim, the someone n=24 TIE presented as the honest overturn it is. Serves at `/lab` with zero worker changes. **Deploy (`npx wrangler deploy`) is a human act — publish stays OPERATOR-GATED.** Site canon updated (BUILD_LOG.md; that folder is not git). Follow-up noted there: a /lab footer link on the essay at the next v-build.

## PUBLISHED 2026-07-09 (explicit operator instruction: "ok publish it all")
**Repo PUBLIC + current:** https://github.com/bochen2029-pixel/ORRERY (was already public at the 6-tool state; Wave-0 arc `43d7613..705b857` pushed after a benign-confirmed secrets sweep). **`/lab` LIVE:** https://finaltheoryofeverything.org/lab (wrangler `b5f78510`; verified live — 200, title, 8x CONFORMANT, all eight golden hashes, essay untouched). Standing rule update: the publish gate is SPENT — pushes at save points are now routine sync; creating NEW public artifacts (new repos, new pages) still asks first.

## Wave 1 OPENED: `hsmi-stab` — contract committed, then the model RETURNED TO DRAFT by its own probe (D-027)
D-026 adopted Active (per-tool opening protocol). Contract v1.0.0 + schema + MODULE committed (`1996d4d`), walking skeleton built on liborrery + cuSOLVER. **Then the discipline fired:** the in-session probe (env `HSMI_PROBE=1`) measured the violation EXACTLY ±t-symmetric at every t and n — theorem: real Hamiltonian ⇒ T-invariant state ⇒ `U(−t)=conj(U(t))` ⇒ the site-basis leak norm cannot see the arrow. **Model withdrawn BEFORE any golden froze (D-027; contract carries the banner; NO GOLDEN EXISTS).** Infrastructure battery green (KAT, covariance projector, many-body ground-energy 1e−9, negative control, determinism both families). **Oracle FIXED (2026-07-10, v1.1 session step 1):** the Fock-vs-Gaussian residual was a back-rotation index bug in the oracle's σ_t assembly (`V·ỹ·V` for `V·ỹ·Vᵀ`); printed first (err 4.9e−1, ±t-symmetric, ~t-independent — the scrambled-back-rotation signature), then fixed: agreement **3.4e−12** vs tol 1e−8, both t's, both directions. The Fock path now pins every Gaussian-engine convention.

**Then the v1.1 session's step 2 (model design) produced the deeper finding — D-028: the FUNCTIONAL is blind, not just the model.** The v1.0.0 leak-norm δ± is identically ±t-symmetric for every model (`spec(MM†)=spec(M†M)`; one-sided subspace containment does not exist in finite D — D-027's measured symmetry was over-determined). The natural successors each die too: Frobenius ax+b witness (trace cyclicity, theorem), spectral ax+b witness (flip×conj congruence of any Toeplitz window — and chirality forces translation invariance), transport element (PH self-conjugacy of half filling, 6 digits), literal Wiesbrock minspec-with-constants (strongly asymmetric in BOTH models — control ratio 66 > chiral 38: nesting monotonicity, not the arrow). Log-lattice naive sampling: positivity clean (margins 0.04–0.12, clamp never active) but UV mangled (m-independent spectra/flow). Probes kept in-code (`HSMI_PROBE=1..6`). **The replacement witness re-defines what F-K1 firing means → escalated: QUESTIONS.md Q-001 (OPEN). hsmi-stab is DEFERRED-ON-QUESTION; no golden exists; nothing citable.** The keystone's first real outputs are *findings about proxy design*: half-sidedness needs chirality (D-027), and the finite-D arrow is protected against the entire unitarily-invariant functional class (D-028).

## Next concrete action
**Q-002 RULED (operator, 2026-07-10): science-handback — DELIVERED** (`runs/hsmi-stab_k1_witness_handback.md`; QUALIA_LAB-facing, claim-by-claim, reproduction-pinned to commit c9ceedb). **hsmi-stab is PARKED** pending the science's reformulation of F-K1's finite-D projection (the memo's §4 lists the asks + the three-control gauntlet any new witness must pass). Wave 1 continues around the keystone: next build is **`trace-born`** (C-TRACE, D-026 order book, contract-first) — or the `someone` fp64 oracle side item (I-11/D-025) if a shorter session is preferred. Both are clean fresh-session starts.
Side candidate when convenient: `someone`'s owed fp64 CPU oracle (I-11/D-025). Publish is DONE; save-point pushes are routine.

## Guards (never violate)
- Contracts and goldens are FROZEN; migrations are [BEHAVIOR-NEUTRAL] by definition or they are rejected.
- Determinism or it doesn't ship. No float atomics in declared reductions. Fast-math banned (D-021/I-13).
- Never conflate exit 1 (gate fired = real result) with exit 2 (error).
- Sims prove STRUCTURE, never ACQUAINTANCE (qualia). §III-sealed.
- Atomic commits: code + canon together (Invariant 10).

## Pointers
Spec: ARCHITECTURE.md (§5 invariants 1–14; §8 has the I-11 oracle column) · Plan: TASKLIST.md (Phases 4+5 COMPLETE; Phase 6 / Wave 1 next) · Decisions: DECISIONS.md (D-022 newest) · Wave plan: docs/PROPOSAL_2026-07-09_wave_plan.md · Lib: lib/MODULE.md · Runbook: BUILD.md · Harness: harness/verify.py (tools only; lib selftest runs standalone from repo root) · Public: github.com/bochen2029-pixel/ORRERY + finaltheoryofeverything.org/lab.
