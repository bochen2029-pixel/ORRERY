# generated build artifacts (regenerated by the build; source + contract + golden are load-bearing)
lens_device.ptx
lens_device_ptx.h
# render outputs (--render is NON-declared, Invariant 3)
*.ppm
*.png
